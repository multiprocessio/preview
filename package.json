{
  "name": "preview",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "A large-object previewer",
  "author": "Phil Eaton",
  "main": "dist",
  "scripts": {
    "typecheck": "yarn tsc",
    "format": "yarn prettier --write \"**/*.js\" \"**/*.ts\" \"**/*.json\"",
    "test": "yarn jest",
    "build": "yarn esbuild preview.ts --sourcemap --outfile=dist/index.js",
    "test-coverage": "yarn jest --coverage"
  },
  "devDependencies": {
    "esbuild": "^0.12.7",
    "esbuild-jest": "^0.5.0",
    "jest": "^27.0.6",
    "prettier": "^2.3.0",
    "typescript": "^4.4.0-beta"
  }
}
